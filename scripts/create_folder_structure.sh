#!/usr/bin/env bash

mkdir seeds
mkdir configs
mkdir models
mkdir crawls
mkdir image_space

chmod a+rwX -R seeds
chmod a+rwX -R models
chmod a+rwX -R configs
chmod a+rwX -R crawls
chmod a+rwX -R image_space